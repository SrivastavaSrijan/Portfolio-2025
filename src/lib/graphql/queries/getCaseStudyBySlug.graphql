query GetCaseStudyBySlug($slug: String!) {
  CaseStudies(where: { slug: { equals: $slug } }) {
    docs {
      title
      experience {
        title
        role

        startDate
        endDate

        id
      }
      tags {
        name
      }
      content
      summary
      illustration {
        url
      }
      updatedAt
    }
  }
}
